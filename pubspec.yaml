name: task_cli
description: Application CLI de gestion de tâches en Dart pur.
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # Pas de dépendance externe requise pour la logique métier

dev_dependencies:
  lints: ^5.1.1
  test: ^1.24.0
